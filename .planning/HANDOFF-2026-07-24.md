# Handoff prompt — telegramVoiceTranscriber, 2026-07-24

Paste everything below into a new LLM session to resume with full context.

---

PROJECT: telegramVoiceTranscriber (github.com/trikiman/telegramVoiceTranscriber)
GSD project — read .planning/STATE.md, ROADMAP.md, REQUIREMENTS.md first.

STATUS: Milestone v1.3 "Active VPN Bot Harvester" (Phase 9) is COMPLETE and
FULLY AUTONOMOUS. No action needed on it unless something breaks. Details below
for context / troubleshooting only.

## What v1.3 does
`scripts/harvest_vpn_bots.py` actively searches Telegram (global search,
subscribed-channel feeds, sponsored ads) for VPN-trial bots, judges offers with
an LLM (`finder/judge.py`), and files up to 5 qualifying bots (guaranteeing
≥1 30-day trial) into the Telegram folder "10+ days vpn" — `/start` + mute +
add to folder. Runs on a SEPARATE Telegram account (finder account,
phone +79958993023) from the main DM-transcription userbot.

## Commit history (all on origin/master, verify with `git log --oneline -10`)
- `69331ef` fix(09): make the VPN bot harvester actually work — fixed the
  original crash-on-import bug, wrong hardcoded folder title, folder-clobber
  bug (2nd bot added would erase the 1st), added FloodWait handling.
- `e23d4ba` docs(09): live-verify HARVEST-05, complete milestone v1.3 — first
  live run from a LOCAL dev machine (Windows), collected 5/5 bots (all
  30-day) into folder id=13 "10+ days vpn" (12→17 peers).
- `06366d7` fix(09): dedupe harvested offers via finder.db found_offers —
  found and fixed a gap where the harvester never wrote to the
  `found_offers` dedupe table, so repeat runs would have re-judged/re-started
  the same bots. Added 4 tests (152 total).
- `9835aeb` feat(09): add systemd timer for daily autonomous harvest run —
  `deploy/tg-voice-harvester.service` (oneshot) + `.timer` (daily 10:00 UTC
  ± 2h random spread, Persistent=true).
- `48a6497` docs(09): document VPS deployment + autonomous daily harvest —
  final STATE.md/ROADMAP.md/09-01-SUMMARY.md sync.

## VPS state (verified via SSH as of 2026-07-24 ~09:35 UTC)
- Host: Oracle Ubuntu `158.101.214.234`, SSH key `E:\Projects\vless\oracle_vless_key`,
  user `ubuntu`. Code at `/opt/tg-voice-transcriber` (now a REAL git checkout
  of origin/master — was hand-copied files before this session, backed up at
  `/opt/tg-voice-transcriber.bak-20260724090819` if a rollback is ever needed).
- `tg-voice-harvester.timer` is `enabled` + `active`, confirmed via
  `systemctl list-timers` — next run scheduled, runs daily unattended.
- Finder session/db at `/var/lib/tg-voice-transcriber/finder.session` (0600,
  tgbot:tgbot) and `finder.db`.
- `/etc/tg-voice-transcriber/env` has finder vars appended, notably
  `TG_VOICE_FINDER_ENABLED=false` — this is DELIBERATE: it disables the
  passive finder scheduler embedded in the main `tg-voice-transcriber.service`
  so it never opens a second Telegram client on the same finder session as
  the standalone harvester script (that pattern is exactly what caused the
  main account's AuthKeyDuplicatedError — see Known Issue below).

## Known issue — UNRELATED to the harvester, still open
Main DM-transcription userbot (phone `+79166076650`, SEPARATE account from the
finder) has session `AuthKeyDuplicatedError` — a local script touched the same
`.local/userbot.session` file while the VPS service was running it, so
Telegram revoked the shared key. `tg-voice-transcriber.service` on the VPS is
currently STOPPED (not crash-looping, was stopped deliberately to avoid
hammering Telegram).

Fix (needs interactive user action, cannot be done by an agent alone):
1. On the LOCAL dev machine: `python scripts/login.py` (fresh SMS/2FA prompt).
2. `scp` the new `.local/userbot.session` to the VPS:
   `/var/lib/tg-voice-transcriber/userbot.session` (owner `tgbot:tgbot`, mode `0600`).
3. `sudo systemctl start tg-voice-transcriber` on the VPS.
4. Going forward: never run local scripts against `.local/userbot.session`
   while the VPS service is live on that same account.

Full details: `.planning/STATE.md` → Known Issues / Debt section.

## Safety rules (non-negotiable, from .planning/research/PITFALLS.md)
- Never hammer retries on FloodWaitError; respect `exc.seconds`.
- Keep `/start` budget ≤15/run, human-like delays; always mute started bots.
- Never run two sessions of the same account (AUTH_KEY_DUPLICATED) — this is
  exactly what broke the main account; don't repeat it for the finder account.
- Session files and API keys never leave the VPS / never get committed to git.

## If asked to check on the harvester
```bash
ssh -i "E:\Projects\vless\oracle_vless_key" ubuntu@158.101.214.234 \
  "systemctl list-timers tg-voice-harvester.timer; sudo journalctl -u tg-voice-harvester --no-pager -n 40"
```

## Reference docs
- `.planning/STATE.md` — current state, known issues
- `.planning/ROADMAP.md` — phase/plan history
- `.planning/REQUIREMENTS.md` — HARVEST-01..05, all satisfied
- `.planning/milestones/v1.3-phases/09-harvester/09-01-PLAN.md` and
  `09-01-SUMMARY.md` — full technical detail of this milestone
- `deploy/README-deploy.md` — VPS operations reference
